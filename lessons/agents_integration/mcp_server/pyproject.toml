[project]
name = "composed-mcp-server"
version = "0.1.0"
description = "Composed MCP server combining Nova and Brown"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.4.0",
    "pydantic-settings>=2.10.0",
]

